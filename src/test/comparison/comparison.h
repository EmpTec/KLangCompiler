#include <stdbool.h>

int eq(int x, int y);
int neq(int x, int y);
int lt(int x, int y);
int lte(int x, int y);
int gt(int x, int y);
int gte(int x, int y);

bool and(bool a, bool b);
bool or(bool a, bool b);
bool not(bool a);